# 后台 Demo 项目说明

## 技术栈

React + TypeScript + Vite + Ant Design。项目使用 Hash 路由和本地 Mock 数据，可构建为纯静态文件。

## 开发前检查

先阅读 `src/App.tsx`、`src/pages/`、`src/mock/`、`src/theme.ts` 和实际路由。文档可能落后于代码，以代码现状为准。

## 常用命令

```bash
npm run dev
npm run check
npm run build
npm run preview
```

## 开发约定

- 优先使用 Ant Design 和 `@ant-design/icons`。
- 页面放在 `src/pages/`，Mock 数据放在 `src/mock/`。
- 稳定的设计值放在 `src/theme.ts`，页面特殊样式放在有作用域的页面类下。
- 默认不连接真实后端，不增加与演示无关的依赖和功能。
- 有实质进度后更新下方记录。

## 当前进度

原型：`增加溢价原型.html`（小蜜蜂自营后台后台化 Demo）。范围：成员管理 / 推广方管理 / 结算中心三个模块；订单管理模块不做（订单数据仅由 `src/mock/engine.ts#buildOrders` 派生供结算使用）。

- 框架：Hash 路由 + 主题（`src/theme.ts`，主色 #2776FF）+ 深色 220px 侧栏 + 58px 顶栏，`src/App.tsx` 统一页面标题。
- Mock：`src/mock/`（data/constants/types/engine/store）。engine 提供容量校验、点级保底议价试算、订单派生、三视图结算行（`buildSettlementRows`）与账单详情计算。
- 成员管理 `src/pages/MemberManagementPage.tsx`：成员列表筛选/分页；640px 抽屉新增/编辑；商家账号（收款/分账/拍摄点分成含按保底价与试算/银行账户）、渠道账号配置；保存走 `validateMemberConfig` 强警告确认。
- 推广方管理 `src/pages/PromotionManagementPage.tsx`：审核态 Tab + 状态/关键词筛选；860px 抽屉新增/编辑（营业执照、收款账户、分成规则容量提示），审核-保存即通过；启用/禁用 pill。
- 结算中心 `src/pages/SettlementPage.tsx` + `src/pages/BillDetailPage.tsx`：线下对公结算 / 线上自动分账 / 推广方结算三个页签 + 视角下拉 + 筛选；申请结算（上传发票→审核中，经 store 本地改写）与发票下载弹窗；账单详情为整页替换（面包屑 + 蓝色账单指标卡 + 订单明细），`/settlement/bill/:view/:billId` 路由。已移除平台结算规则说明区，列表与账单明细采用紧凑表格及分组信息布局，详情保持二级页面而非右抽屉。推广方与渠道分成配置统一为空规则初始态，点击新增后生成规则；账单统计支持左侧景区单选范围筛选并联动明细与统计数据，明细恢复九列且内容不加粗。订单号由订单生成层按下单时间补齐为 `YYYYMMDDHHMMSS`，金额右对齐、文本/时间/编号左对齐。溢价归收款商户，结算数据记录并在统计栏展示基础分成、溢价及应结算金额拆分，明细规则列按订单始终展示基础+溢价拆分（含 0 值）；结算列表与账单明细状态统一居中，正文取消加粗。成员/推广方自定义表单字段、规则和上传控件统一在组件下方显示标准错误提示，结算申请发票上传校验也改为组件下方提示。已移除操作栏上方的表单级错误块，规则错误按行显示在对应控件下方，跨字段业务错误保留顶部 toast。

构建通过：`npm run check` + `npm run build`；`npm run dev` 下三模块页面均已用无头 Chrome 渲染核对（DOM 结构含预期列表/金额/详情行）。最近完成一次测试视角巡检：基础必填/手机号/密码/重复账号、推广方营业执照与分成规则、商户/渠道配置字段级错误提示、空白规则行校验、首尾空格清理、发票上传校验已补齐或修正；跨字段分成容量与配置冲突保留顶部 toast。推广方规则错误文案已移除行号，推广方/渠道/商户分成比例统一允许 0，范围为 0-100%。结算申请操作项统一为链接按钮；修复商户拍摄点超限提示将布尔值误显示为空的问题，并核对其他超限文案均使用数值。推广方审核态已移除顶部说明条，所有表单控件显式禁用，并压缩推广方表单标题及字段间距。所有抽屉表单统一为中等信息密度：区块 24px、字段 12px、标题下方 12px，小节标题 16/10px；规则列表维持紧凑行距。待业务确认项见交付说明：发票金额是否需要真实解析、账号/推广方名称唯一性、银行账号格式及商户号格式规则。
已新增 `docs/溢价配置与结算中心-PRD-20260910.md`：基于 `docs/分成配置-PRD.md` 补充结算中心、账单详情、溢价拆分、发票申请和审核流程，并按当前 Demo 口径统一为七段式 PRD。
已新增并再次简化 `docs/销售对外宣讲-溢价与分成比例说明.md`：面向销售和客户以“谁收款、谁分成、剩余归谁”为主线解释参与方、两种收款模式、0-100% 分成比例、溢价归属和 100 元案例，去除后台操作及内部术语；文档开头增加商务谈判决定具体方案的边界说明。
